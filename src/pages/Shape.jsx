import React from 'react';
import { shape } from '../assets/svg';
import Layout from '../components/Layout';

function Shape() {
  return (
    <Layout title="Shape" icon={shape}>
      Shape Content
    </Layout>
  );
}

export default Shape;
