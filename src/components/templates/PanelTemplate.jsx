import styled from 'styled-components';

const PanelTemplate = styled.article`
  flex-grow: 3;
  position: relative;
  height: 100vh;
  background-color: #f1f2f6;
  box-shadow: 0 0 10px rgba(0, 0, 0, 0.3);
`;

export default PanelTemplate;
